# TRAILCHAIN

A fast Snake-like game where your run becomes the next player's maze.

## Setup

```bash
npm install
npm run dev
```

## Deploy

Connected to Vercel — pushes to `main` branch auto-deploy.

## Stack

- React + Vite
- Supabase (database)
- Vercel (hosting)
